import { START_FETCH_ARTIST,
  FETCH_ARTISTS,
  RECEIVE_ARTISTS,
  START_FETCH_ARTIST_INFO,
  RECEIVE_ARTIST_INFO,
  FETCH_ARTIST_INFO 
} from '../actions/artists'

/* Initial state object will have this shape */
const initialState = {
  
}